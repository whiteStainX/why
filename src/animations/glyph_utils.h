#pragma once

#include <string>
#include <vector>

namespace why {
namespace animations {

std::vector<std::string> parse_glyphs(const std::string& source);

} // namespace animations
} // namespace why

